 que você está fazendo:
Você está migrando um sistema Flutter de cores estáticas (AppThemeColors. xxx) para cores dinâmicas (ThemeColors.of(context).xxx) usando um InheritedWidget, permitindo que o usuário troque o tema de cores em tempo real.

Arquitetura:
Dart
// ANTES (Estático - funciona em qualquer lugar)
Container(color: AppThemeColors.blueMaterial)

// DEPOIS (Dinâmico - precisa de BuildContext)
Container(color: ThemeColors. of(context).blueMaterial)
🔍 Análise dos Problemas Encontrados
Analisei seus arquivos e identifiquei 5 categorias de problemas:

Problema 1: Inicializadores de Campo (CRÍTICO)
Dart
// ERRO:  context não existe em inicializadores de campo
class _MyWidgetState extends State<MyWidget> {
  Color _corSelecionada = ThemeColors.of(context).blueMaterial; // ❌ ERRO
  
  final List<Color> _cores = [
    ThemeColors.of(context).blue,  // ❌ ERRO
    ThemeColors.of(context).green, // ❌ ERRO
  ];
}
Arquivos afetados: category_add_screen.dart, category_edit_screen.dart, category_products_screen.dart, dashboard_screen.dart, dashboard_navigation_rail.dart

Problema 2: Blocos static const
Dart
// ERRO: static const não pode ter valores dinâmicos
static const List<Map<String, dynamic>> _menuItems = [
  {'gradient': [ThemeColors.of(context).x, ThemeColors.of(context).y]} // ❌
];
Arquivos afetados: dashboard_screen.dart (linhas 66-111)

Problema 3: Métodos sem BuildContext
Dart
// ERRO: Método não recebe context
Widget _buildHeader() {
  return Container(color: ThemeColors. of(context).blue); // ❌ context não existe
}
Arquivos afetados: Vários widgets com métodos _buildXXX

Problema 4: const com valores dinâmicos
Dart
// ERRO: const não pode conter chamadas de método
const BoxDecoration(
  gradient: LinearGradient(
    colors:  [ThemeColors. of(context).green, ... ], // ❌
  ),
)
Problema 5: Widgets StatelessWidget sem context em campos
Dart
// Arquivos como compact_metrics_grid.dart, fluxos_inteligentes_card.dart
// que definem cores em campos sem acesso a context
🛠️ Solução Proposta: Script Versão 8.0
Estratégia em 6 Fases:
FASE 1: Identificar e classificar cada ocorrência de AppThemeColors
FASE 2: Converter static const com cores para static final com getters
FASE 3: Converter inicializadores de campo para late final ou mover para didChangeDependencies
FASE 4: Adicionar BuildContext a métodos que precisam
FASE 5: Migrar ocorrências válidas
FASE 6: Remover const de blocos afetados

Como Executar
Passo 1: Fazer backup
PowerShell
Copy-Item "d:\tagbean\frontend\lib\modules" "d:\tagbean\frontend\lib\modules_backup_v8" -Recurse
Passo 2: Rodar em modo análise primeiro
PowerShell
.\migrate_theme_colors_v8.ps1 -AnalyzeOnly -Verbose
Passo 3: Rodar em modo DRY RUN
PowerShell
.\migrate_theme_colors_v8.ps1 -DryRun -Verbose
Passo 4: Executar migração real
PowerShell
.\migrate_theme_colors_v8.ps1 -Verbose
Passo 5: Verificar erros
PowerShell
cd d:\tagbean\frontend
flutter analyze lib\modules
⚠️ Casos que Precisam de Refatoração Manual
O script não migra automaticamente os seguintes casos (marca com TODO):

static const com cores - Precisa converter para getter ou método
Inicializadores de campo - Precisa mover para didChangeDependencies()
Listas de cores em campos - Precisa criar getter
Exemplo de refatoração manual para dashboard_screen.dart:
Dart
// ANTES (não funciona)
static const List<Map<String, dynamic>> _menuItems = [
  {'gradient': [AppThemeColors.moduleDashboard, AppThemeColors. moduleDashboardDark]},
];

// DEPOIS (funciona com context)
static List<Map<String, dynamic>> getMenuItems(BuildContext context) => [
  {'gradient': [ThemeColors.of(context).moduleDashboard, ThemeColors.of(context).moduleDashboardDark]},
];